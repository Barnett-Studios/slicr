{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://barnett.studio/schemas/execution-manifest.schema.json",
  "title": "execution-manifest",
  "description": "The Slicr's output contract: a plan embeds one fenced JSON block {\"execution-manifest\": [entry, ...]}. The reference producer/validator is plan_to_nodes.py; an executor consumes the local:true entries as RED-gated nodes.",
  "type": "object",
  "required": [
    "execution-manifest"
  ],
  "properties": {
    "execution-manifest": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/entry"
      }
    }
  },
  "additionalProperties": true,
  "$defs": {
    "entry": {
      "type": "object",
      "required": [
        "id",
        "files",
        "change",
        "accept"
      ],
      "additionalProperties": false,
      "properties": {
        "id": {
          "type": "string",
          "minLength": 1,
          "maxLength": 100,
          "pattern": "^[A-Za-z0-9][A-Za-z0-9._-]*$",
          "description": "Node id; becomes the NN-<id>.json filename. Must be a path-safe slug: no path separators and no leading dot, so '..' and '.' are unrepresentable and the id can never traverse out of the output directory. The reference validator (plan_to_nodes.py) applies the same rule with re.fullmatch and is the authority where dialects differ on a trailing newline."
        },
        "files": {
          "type": "array",
          "items": {
            "type": "string",
            "minLength": 1,
            "pattern": "^[^~/]",
            "description": "A repo-relative editable path. The pattern enforces the two rules a lookahead-free regex engine can express — no leading '/' and no leading '~' (which expanduser and shell tilde expansion resolve outside the repo). 'No .. component' still cannot be expressed here: it needs a negative lookahead, which the Rust regex crate does not support, so the reference validator remains authoritative. A producer trusting the schema alone can still emit a '..' path."
          },
          "minItems": 1,
          "description": "Editable files the node may change. Must be relative paths inside the repo: no leading '/', no '..' component. Enforced by the reference validator rather than by a pattern here — expressing 'no .. component' needs negative lookahead, which the Rust regex engine used elsewhere in this family does not support, and an unenforceable-by-some-consumers pattern is worse than a documented rule."
        },
        "change": {
          "type": "string",
          "description": "The task description shown to the executing model."
        },
        "accept": {
          "type": "string",
          "minLength": 1,
          "description": "Shell command; exit 0 iff the node is solved (the RED oracle)."
        },
        "kind": {
          "enum": [
            "edit",
            "create"
          ],
          "default": "edit",
          "description": "edit an existing region, or create a new single file."
        },
        "forbid": {
          "type": "array",
          "items": {
            "enum": [
              "new_deps"
            ]
          },
          "uniqueItems": true,
          "description": "Constraints the change must not violate."
        },
        "local": {
          "type": "boolean",
          "default": false,
          "description": "Offload to an executor as a node. Only local:true entries become nodes; local:false is executed inline by the planning model."
        },
        "base_state": {
          "type": "object",
          "minProperties": 1,
          "additionalProperties": false,
          "properties": {
            "git_commit": {
              "type": "string",
              "minLength": 1,
              "description": "Commit the plan was authored against."
            },
            "tree_hash": {
              "type": "string",
              "minLength": 1,
              "description": "Hash of the tree the plan was authored against."
            }
          },
          "description": "Optional. The repository state this entry was planned against, so an executor can tell a node applied to the tree it was written for from one applied to a tree that has moved under it. Neither key is pattern-constrained: 'a 40-hex sha' is false for an abbreviated rev and for a sha256 repository, and a producer whose legitimate value the schema rejects is a worse failure than a malformed one it accepts. minProperties: 1 — an empty object is a binding that binds nothing, and a consumer checking 'does base_state match' would pass it vacuously. What an executor DOES on a mismatch is policy and lives in the consumer, deliberately not here."
        }
      },
      "allOf": [
        {
          "if": {
            "properties": {
              "kind": {
                "const": "create"
              }
            },
            "required": [
              "kind"
            ]
          },
          "then": {
            "properties": {
              "files": {
                "maxItems": 1
              }
            }
          }
        }
      ]
    }
  }
}
